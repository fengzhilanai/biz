{
  "name": "fengzhilanai-biz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "preview": "python3 -m http.server 8000",
    "check": "python3 -c \"from pathlib import Path; required = ['index.html', '404.html', 'assets/styles.css', 'assets/main.js', 'assets/logo.png', 'assets/logo-white.png', 'assets/favicon.png', 'assets/og-image.png']; missing = [p for p in required if not Path(p).is_file()]; assert not missing, f'缺少静态文件: {missing}'; print('静态文件检查通过')\""
  }
}
